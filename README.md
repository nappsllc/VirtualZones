# VirtualZones

Public releases-only companion repo for [VirtualZones](https://github.com/nappsllc/VirtualZones-private)
(a Windows window-management utility). This repo hosts signed release binaries and
`update.json` manifests used by the app's in-app auto-update check — it contains no source
code.
